{
  "name": "pi-extension-ask-plan",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "pi --no-session --no-extensions -e ./index.ts --help >/dev/null 2>&1 && node --import ./test/register-ts-loader.mjs --test test/*.test.ts"
  }
}
